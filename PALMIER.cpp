#include "Parser.hpp"
#include "Ilian.hpp"
#include "PALMIER.hpp"


void intersection(int int1, std::string namefrérot)
{
    this->name = int1;
    this->street = (namefrérot + " ");
}

void ~intersection()
{
    //DESTROY ANYTHING
}

void assemblage()
{
    for (int x = 0 ; x < "LE NB D'INTERSECTION" ; x++) {
        int first_intersection = Allstreet[x]._intersection1;
        int second_intersection = Allstreet[x]._intersection2;
        if (intersection.name = first_intersection1)
            intersection.street = (Allstreet[x]._name + " ");    
        else if (intersection.name = first_intersection2)
            intersection.street = (Allstreet[x]._name + " ");
        else
            intersection.intersection(Allstreet[x].__intersection1, Allstreet[x]._name + " ");
            intersection.intersection(Allstreet[x].__intersection2, Allstreet[x]._name + " ");
    }

    for (int x = 0 ; x < "LE NB DE INTERSECTION" ; x++) {
        /*
            Faut marquer le nb d'intersection et le nb de rue 
            ensuite mettre toute les rues les rues entre intersection puis 5 sec
        */
    }
}